local status, mason = pcall(require, "mason")
if not mason then
  print("mason missing")
  return
end

mason.setup({
    ui = {
        icons = {
            package_installed = "✓",
            package_pending = "➜",
            package_uninstalled = "✗"
        }
    }
})
